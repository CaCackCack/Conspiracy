


(if (environment-bound? (nearest-repl/environment) '__R_SKY__) (define __R_SKY__) )


(define SKYFLATNAME "F_SKY1")

(define ANGLETOKEYSHIFT 22)

(define skytexture)
(define skytexturemid)

(define (R_InitSkyMap ) )


